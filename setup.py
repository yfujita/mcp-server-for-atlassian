"""Setup script for mcp-server-for-atlassian.

This file is kept for compatibility with tools that don't fully support
pyproject.toml yet. The actual configuration is in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
